# Dependencies
node_modules

# Build output
dist
out
.vite

# Native module build artifacts
**/build/Release
**/prebuilds

# Caches
.eslintcache
.cache
.parcel-cache
coverage
*.tsbuildinfo

# Logs
*.log
*.log.*

# Editor / OS
.DS_Store
Thumbs.db
.vscode
.idea
*.iml
*.bak
*.swp
*~

# Secrets and env
.env
.env.*
!.env.example

# User data / wallet state — NEVER publish. The app writes these at runtime
# and they contain encrypted private keys, salts, and session metadata.
*.db
*.db-journal
*.sqlite
*.sqlite3
_ww_export.json
wartwallet.db

# Transient / debug artifacts produced during local development
asar-tmp
.icon-ico
*.original

# Lock files (project policy: checked in locally only)
package-lock.json
pnpm-lock.yaml
bun.lock
